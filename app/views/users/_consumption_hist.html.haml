#consumption_history
  %h3 Drugs this week
  - if @history.empty?
    %p.gray None yet!
  - @history.each do |consumption_record|
    %p
      = consumption_record.dose_description
      %span.date= consumption_record.created_at.strftime("%m/%d/%y")


